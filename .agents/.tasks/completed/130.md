---
id: 130
title: add integration-level CLI smoke tests
status: Completed
priority: P2
effort: M
labels: type:test,area:cli,area:tests
exec_plan: -
depends_on: -
created: 2026-06-26T17:41:34-04:00
updated: 2026-06-26T22:11:04-04:00
---
# add integration-level CLI smoke tests

## Summary

All tests in `internal/ahm/*_test.go` are Go unit tests calling functions directly. There are no tests that build the `ahm` binary and invoke it as a subprocess in temp directories. CLI contract changes — flag parsing, exit codes, help text format, command wiring — can reach users without automated detection. The `just smoke-agents` recipe exists for agent integration testing but covers only the `task work` orchestration path.

## Evidence

- `internal/ahm/*_test.go` — all tests import `ahm` package and call `Main()`, `parseTask()`, etc. directly. No `os/exec` invocations of the built binary.
- `cmd/ahm/main.go` — the entry point wraps `ahm.Main()` in `os.Exit`. No test file in `cmd/ahm/`.
- Recent CLI changes (exit code documentation in task 129, search command addition, `--priority`/`--effort` filters) were tested only through unit tests of the internal functions, not through real CLI invocations.

## Fix Direction

Add `internal/ahm/cli_integration_test.go` (or `cmd/ahm/integration_test.go`). Build the binary once with `go build -o <tmpdir>/ahm ./cmd/ahm` in `TestMain`, then run it as a subprocess with `os/exec` in temp directories.

Key scenarios to cover (reference `docs/cli.md` and `CONTRIBUTING.md` for expected behavior):

1. **Exit codes**: `ahm` (no subcommand in managed dir) exits 0 on clean state, 1 on validation errors. `ahm --help` exits 0. Unknown subcommand exits 2.
2. **Task lifecycle**: init → create → list → show → accept → start → complete. Verify file exists at expected path, verify index is regenerated.
3. **Output modes**: `--json` produces valid JSON with expected fields. `--plain` produces compact JSON. `--text` (default) produces human-readable output.
4. **`--dry-run`**: create, complete, cancel in dry-run produce preview output without mutating disk.
5. **Error messages**: invalid flag values produce usage errors with example invocations.

Use `t.TempDir()` for isolation. Do NOT invoke real external agents (no `task work` tests). Do NOT test `just smoke-agents` scenarios — those are separate.

## Acceptance Notes

- [x] `go test -count=1 ./internal/ahm -run Integration` passes.
- [x] Tests cover: exit codes (0, 1, 2), task init→complete lifecycle, `--json`/`--plain`/`--text` output modes, `--dry-run`, and usage error messages.
- [x] Tests use `t.TempDir()` and do not touch the real repository.
- [x] `just ci` passes.

