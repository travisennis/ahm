---
id: 037
title: Remove duplicate argument checks from task handlers
status: Completed
priority: P3
effort: S
labels: type:refactor, area:cli
exec_plan: -
depends_on: -
updated: 2026-06-06T13:23:13-04:00
---
# Remove duplicate argument checks from task handlers

## Summary

Review issue 37. Several task handlers re-check argument counts already enforced by Cobra. Acceptance: pure handler functions accept typed inputs or the redundant checks are removed where Cobra guarantees arity, with command tests passing.

## Acceptance Notes

- [x] Removed redundant `len(argv) != 1` check from `taskShow`
- [x] Removed redundant `len(argv) != 1` check from `taskStatus`
- [x] Removed redundant `len(argv) != 2` check from `taskDepUpdate`
- [x] Removed redundant `len(argv) != 1` check from `taskDepTree`
- [x] All command tests pass

