---
id: 158
title: Add docs-md-lint to just ci
status: Completed
priority: P2
effort: M
labels: type:task,area:ci
exec_plan: -
depends_on: -
created: 2026-07-02T08:10:43-04:00
updated: 2026-07-02T20:33:35-04:00
---
# Add docs-md-lint to just ci

**Problem:** `docs/guardrails/documentation.md` (Enforcement section) records that markdownlint runs only via `just docs-md-lint` and "is not yet in `just ci` — there is a baseline of pre-existing issues that must be cleaned up before it can block CI." That gap was written down and never scheduled; CI currently runs no doc checks at all.

**Fix:** Clean up the pre-existing markdownlint baseline, add `docs-md-lint` to `just ci` (and CI workflow if `just ci` is not what CI runs), and update the Enforcement section of the documentation guardrail to reflect that the check now blocks.

**Verification:** A deliberate markdownlint violation fails `just ci`; the guardrail's Enforcement section no longer describes the check as pending.

## Acceptance Notes

- [x] Cleaned up all 22 pre-existing markdownlint errors across 5 files:
  - `docs/guardrails/dependencies-build-ci-release.md` — fixed table separator (MD060)
  - `docs/references/cli/global-contract.md` — added `text` language to 2 fenced code blocks (MD040)
  - `internal/templates/workflow/finding-improvements-SKILL.md` — added `text` language to 2 fenced code blocks (MD040)
  - `internal/templates/workflow/grooming-backlog-SKILL.md` — added `text` language to 1 fenced code block (MD040)
  - `internal/templates/workflow/preflight-SKILL.md` — fixed table style to compact (MD060)
- [x] Added `docs-md-lint` to the `ci` recipe in `justfile` (runs after `vuln`, before `build`)
- [x] Updated `docs/guardrails/documentation.md` Enforcement section to state the check "runs as part of `just ci` and blocks the pipeline on any markdownlint error."
- [x] Verified `just docs-md-lint` exits 0 (0 errors)
- [x] Verified `just ci` exits 0 (full pipeline including docs-md-lint passes)

