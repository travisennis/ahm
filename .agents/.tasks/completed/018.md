---
id: 018
title: Make task front matter migration index updates robust
status: Completed
priority: P2
effort: S
labels: type:bug, area:tasks
exec_plan: -
depends_on: -
updated: 2026-05-31T18:50:03-04:00
---
# Make task front matter migration index updates robust

## Summary

Review issue 18. migrateTaskFrontMatter mutates lines and reuses indexes in a way that could corrupt future changes. Acceptance: migration recomputes indexes after each mutation or applies collected operations safely, with tests covering multiple inserted and normalized fields.

## Acceptance Notes

- [x] Refactored `migrateTaskFrontMatter` to recompute the line index after every mutation (`index = frontMatterLineIndex(lines)`), making it robust against future code changes that might alter line count.
- [x] Added `TestMigrateTaskFrontMatter_MultipleInsertAndNormalize` with 5 sub-tests covering: missing labels + multiple normalizations + depends_on normalization; already-normalized no-op; only-labels-missing; effort placeholder with already-valid priority; and depends_on follows-form combined with priority normalization.
- [x] All existing tests pass without modification.
- [x] Full CI passes.

