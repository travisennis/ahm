---
id: 035
title: Avoid copying seen map on every dependency tree recursion
status: Completed
priority: P2
effort: S
labels: type:perf, area:tasks
exec_plan: -
depends_on: -
updated: 2026-06-03T21:06:02-04:00
---
# Avoid copying seen map on every dependency tree recursion

## Summary

Review issue 35. taskDepTree copies the seen map at every recursion, creating avoidable O(depth squared) memory behavior. Acceptance: use backtracking or a path representation to track cycles, preserving output and tests.

## Acceptance Notes

- [x] Replaced `nextSeen` map copy in `taskDepTree` with in-place backtracking (set `seen[id] = true` before recursion, `delete(seen, id)` after)
- [x] All existing tests pass unchanged: `TestTaskDependencyTreeOutput`, `TestTaskDepCyclesCommand`, `TestTaskDependencyCycleDetection`
- [x] Full CI (`just ci`) passes clean

