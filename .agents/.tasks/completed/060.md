---
id: 060
title: Fix release version injection broken by const templates.Version
status: Completed
priority: P1
effort: S
labels: type:bug, area:release, area:templates, risk:release
exec_plan: -
depends_on: -
created: 2026-06-10T11:27:11-04:00
updated: 2026-06-10T13:26:21-04:00
---
# Fix release version injection broken by const templates.Version

## Problem

`.goreleaser.yaml` injects the release version with
`-X github.com/travisennis/ahm/internal/templates.Version={{ .Version }}`, but
`Version` is declared as a `const` (`internal/templates/templates.go:9`). The
linker's `-X` flag only sets `var` symbols, so the injection silently does
nothing. Verified empirically: building with
`go build -ldflags "-X github.com/travisennis/ahm/internal/templates.Version=9.9.9" ./cmd/ahm`
still prints `0.2.0`.

No tags have been pushed yet, so this has not shipped — but the first tagged
release will report `0.2.0` from `ahm --version`, in `ahm status`
`template_version`, and in every consumer's `.agents/ahm.json`.

Task 023 made `Version` a `const` on the stated assumption that there was "no
separate release pipeline that tags differently". That assumption was wrong:
the goreleaser config has injected (or attempted to inject) the tag version all
along.

## Design consideration

`templates.Version` currently serves double duty: CLI binary version and
workflow template version recorded in consumer `ahm.json`. If the fix is simply
`const` -> `var`, every binary release will change what gets stamped into
consumer metadata even when templates did not change.

## Fix Direction

Chosen: **Option 2** — Separate binary and template versions.

- Add an `internal/version` package with a `var Binary` string injected by
  goreleaser, used for `--version`/`ahm version`.
- Keep `templates.Version` as a `const` template-schema version bumped only
  when embedded templates change.
- `ahm.json` `version` keeps tracking the template version.

Rationale: Avoids conflating binary release version with template schema
version. Record the decision in `docs/upgrades.md` since it changes durable
version semantics.

## Relevant Files

- `.goreleaser.yaml` (ldflags)
- `internal/templates/templates.go:9`
- `internal/ahm/cli.go` (root command `Version` wiring)
- `internal/ahm/install.go` (`meta.Version = templates.Version`)
- `internal/ahm/status.go` (`template_version` output)
- `docs/cli.md`, `docs/upgrades.md`
- `.agents/.tasks/completed/023.md` (prior decision being revisited)

## Acceptance Notes

- [x] `just release-check` (goreleaser snapshot) produces a binary whose
      `ahm version` reflects the injected version, verified manually or by test.
      Verified: snapshot build `0.0.1-next` printed correctly from the binary.
- [x] Decision on binary-version vs template-version separation is documented.
      Recorded in `docs/upgrades.md` under "Version Separation (2026-06-10)".
- [x] `docs/cli.md` version/`--version` docs match the new behavior.
- [x] `just ci` passes.

### Changes Made

- Created `internal/version/version.go` with `var Binary = "0.2.0"`
- Updated `internal/ahm/cli.go` to use `version.Binary` for `--version`/`version`
  command (replaced `templates.Version`)
- Updated `.goreleaser.yaml` to inject into `internal/version.Binary`
- Updated `docs/cli.md` — `--version` and `version` command now describe the
  binary version, distinct from the template version in `status`/`doctor`
- Updated `docs/upgrades.md` — recorded the version separation decision
  with rationale and rejected alternative

### Verification

- `go build ./cmd/ahm` succeeds
- `go test ./internal/ahm -count=1 -race` passes
- `go test ./internal/templates -count=1` passes
- `go vet ./internal/... ./cmd/ahm` passes
- `goreleaser snapshot` built binary shows injected version (`0.0.1-next`)
- `status` still correctly reports `template_version: 0.2.0` from the const

