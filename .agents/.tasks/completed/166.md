---
id: 166
title: Add task work model override flag
status: Completed
priority: P2
effort: M
labels: type:feature,type:test,area:cli,area:agent,risk:external-service
exec_plan: -
depends_on: -
created: 2026-07-07T20:21:22-04:00
updated: 2026-07-09T15:57:15-04:00
---
# Add task work model override flag

## Problem

`ahm task work <id>` can select the external coding-agent CLI with `--agent`, but it cannot select the model used by that agent. Users currently have to rely on each provider CLI's ambient configuration, which makes one-off task runs harder to reproduce and prevents `ahm --dry-run task work` from showing the intended model.

Add a `--model <name>` flag to `ahm task work` so a user can request the model for the selected agent for a single invocation.

## Relevant Files

- `internal/ahm/task_commands.go` - Cobra wiring, usage, examples, and flag validation for `task work`.
- `internal/ahm/task_work.go` - task-work argument flow, dry-run preview, prompt handoff, and orchestration entry point.
- `internal/ahm/task_agents.go` - supported agent definitions and provider-specific argument builders.
- `internal/ahm/task_session.go` - review, feedback resume, and commit handoff argument paths that must stay consistent with model selection semantics.
- `internal/ahm/task_commands_test.go`, `internal/ahm/task_commands_golden_test.go`, and `internal/ahm/testdata/agents/` - focused task-work, dry-run, parser, and golden transcript coverage.
- `docs/references/cli/task-commands.md`, `docs/cli.md`, `docs/guides/testing.md`, `docs/references/workflow-spec.md`, and ADR 006 - documented task-work contract and external agent testing expectations.

## Fix Direction

Thread an optional model value through `taskWorkArgs` and the selected `taskWorkAgent` invocation builders. Define and document how each supported agent maps model selection to its CLI flags, and make unsupported or empty model values fail as usage errors only when the selected agent cannot accept a model override.

Decide whether `--model` applies only to the initial implementation session or also to review, feedback resume, and commit handoff. Prefer a consistent per-invocation rule unless a provider CLI makes that impossible, and document the rule clearly. Include the selected model in dry-run output so users can verify the exact handoff before invoking an external agent.

Update ADR 006 or add a focused follow-up ADR before code changes if the implementation changes the durable agent-delegation boundary.

## Acceptance Notes

- [x] `ahm task work <id> --model <name>` is accepted and passed to supported agent CLI invocations using each provider's documented model flag shape.
- [x] Model-selection precedence for this task is explicit: CLI `--model` affects the current run only and does not mutate repository metadata.
- [x] Dry-run output includes the selected model and the final agent arguments that would be executed.
- [x] Review receives the same model override (documented in `task-commands.md`). Feedback resume and commit handoff reuse the existing session which already has the model set — documented rationale.
- [x] All four agents (cake, claude, codex, cursor) support `--model <name>` via their argument builders. Empty model (default) produces no extra flags; non-empty adds provider-appropriate `--model <name>` in positional order.
- [x] CLI docs updated in `docs/references/cli/task-commands.md`. Tests added for model flag in args, dry-run, all agents, and review args. ADR 006 not updated — change is additive (opt-in flag), does not alter the durable agent-delegation boundary established in ADR 006. Workflow spec not updated — no model references beyond the pre-ADR-006 language.

### Live Smoke Evidence

Only codex is available on this dev machine (`codex-cli 0.142.5`). The codex smoke test (`TestAgentSmoke/codex`) fails because `/Users/travisennis/.codex/config.toml` is inaccessible due to macOS security policy — a pre-existing environment issue, not caused by this change. cake, claude, and cursor-agent are not on PATH. The change is additive (optional `--model` flag appended to argument builders); existing argument shapes are preserved when model is empty, so existing agent integration paths are unaffected.

### Preflight

Preflight skill run 2026-07-09. Scale M (multi-file, ~209 insertions, single module). Two review passes completed:
- **Pass 1 (Rules conformance)**: AGENTS.md routing followed (External Agent Orchestration + CLI workflows). Docs updated in `docs/references/cli/task-commands.md`. ADR 006 boundary unchanged (additive flag). Task acceptance notes all checked.
- **Pass 2 (Correctness)**: All four agent arg builders correctly thread model parameter. Empty model produces no `--model` flag. Dry-run includes model in preview. Review receives model override; commit handoff reuses existing session (documented). Tests pass for all agents in work and review argument shapes.
- Validation: `go build ./...`, `go vet ./internal/ahm/`, `gofmt` all clean. Full `go test ./internal/ahm/` passes (26.9s).
- No actionable issues found.

