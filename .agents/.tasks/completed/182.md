---
id: 182
title: Fix task-command tests broken by .ahm-first init
status: Completed
priority: P1
effort: S
labels: type:bug,area:workflow,area:cli
exec_plan: -
depends_on: -
created: 2026-07-12T20:03:55-04:00
updated: 2026-07-12T20:08:50-04:00
---
# Fix task-command tests broken by .ahm-first init

## Summary

Task 175 changed fresh ahm init to create the committed .ahm/ layout, and updated install_test, root_test, indexes_test, validation_test, context_test, and templates_test — but missed the task-command test family. 31 tests in task_commands_test.go, task_deps_test.go, and tasks_test.go use an init/install fixture (now producing .ahm/ layout) while asserting hardcoded .agents/.tasks paths, so go test ./internal/ahm fails on master after commit 376d063.

## Fix Direction

Update the failing tests to assert .ahm/tasks paths (they exercise the modern layout init now creates). Tests that scaffold legacy .agents state directly keep passing and retain legacy coverage.

## Acceptance Notes

- [x] go test ./... -count=1 passes on master (all packages ok)
- [x] Failing tests assert .ahm/tasks paths against the init fixture (48 path updates across task_commands_test.go, task_deps_test.go, tasks_test.go, task_migrate_test.go)
- [x] workflowPathsFor selects the .ahm layout when .ahm/config.json is present but unreadable, so corrupt metadata degrades to warnings instead of "task not found"

## Comments

**2026-07-12T20:08:50-04:00** — Fix surfaced a product bug beyond the tests: workflowPathsFor required a successful metadata read to select the .ahm layout, so a corrupt .ahm/config.json silently flipped record resolution back to legacy .agents/ paths and task commands failed with 'not found' instead of the corrupt-metadata warning. Fixed by keying off the anchoring config file (readMetadataWithSource returns the source path even on read errors); TestTaskCompleteWarnsOnCorruptMetadata and TestTaskWorkWarnsOnCorruptMetadata now exercise the corrupt .ahm/config.json path.

