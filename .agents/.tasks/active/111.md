---
id: 111
title: Move relPath from agents.go to a general utility file
status: Open
priority: P3
effort: S
labels: type:task,area:refactor
exec_plan: -
depends_on: -
created: 2026-06-19T12:02:59-04:00
---
# Move relPath from agents.go to a general utility file

## Summary

The `relPath` function in `internal/ahm/agents.go:92` is a general-purpose path utility used by 10+ callers across 8 files. It lives in `agents.go` — a file otherwise dedicated to the `agents suggestions` command — making it hard to discover and violating the principle that utility functions should live where callers expect to find them.

## Evidence

- `internal/ahm/agents.go:92` — `func relPath(root string, path string) string` defined here but called from:
  - `internal/ahm/lock.go` (lines 30, 36, 40)
  - `internal/ahm/tasks.go:79` (`collectTasks`)
  - `internal/ahm/task_create.go:76-78`
  - `internal/ahm/task_migrate.go:30`
  - `internal/ahm/adrs.go:86` (`collectADRs`)
  - `internal/ahm/adr_migrate.go:51`
  - `internal/ahm/indexes.go:23,57`
  - `internal/ahm/validation.go` (extensively)
  - `internal/ahm/install.go`

## Fix Direction

Move `relPath` to a new file `internal/ahm/path.go` alongside `root.go` (which also deals with path operations). No logic changes — pure code move. Update all callers that currently import or reference it. Since it's in the same package, callers don't need import changes — just moving the definition suffices.

Files in scope: create `internal/ahm/path.go`, edit `internal/ahm/agents.go` to remove `relPath`.
Out of scope: any other refactoring or utility consolidation.

## Acceptance Notes

- [ ] `relPath` is moved from `agents.go` to a new `path.go`
- [ ] All existing tests pass (`go test github.com/travisennis/ahm/internal/ahm`)
- [ ] `just ci` passes

