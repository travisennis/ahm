---
id: 116
title: Add task comment command
status: Open
priority: P2
effort: M
labels: type:feature, area:cli, area:tasks
exec_plan: -
depends_on: -
created: 2026-06-19T14:52:55-04:00
---
# Add task comment command

## Summary

Add `ahm task comment <id> <text>` so humans and agents can append lightweight timestamped notes to an existing task without manually editing the task Markdown file.

## Problem

Tasks are durable working records, but adding progress breadcrumbs currently requires opening and editing the task file by hand. That is workable for large updates, but too heavy for short notes such as discoveries, handoff context, blocked reasons, review observations, or implementation breadcrumbs.

A first-class comment command would make it cheap to preserve context across sessions while keeping task files as the source of truth.

## Relevant Files

- `internal/ahm/task_commands.go`
- `internal/ahm/tasks.go`
- `internal/ahm/task_status.go`
- `internal/ahm/output.go`
- `internal/ahm/task_commands_test.go`
- `docs/references/cli/task-commands.md`
- `docs/references/cli/task-file-format.md`
- `docs/references/workflow-spec.md`
- `ARCHITECTURE.md`

## Proposed Direction

Add a `task comment` subcommand that resolves the task ID using the same rules as `task show`, appends a comment to a dedicated body section, preserves front matter and unknown fields, updates the task's `updated` timestamp, and writes the task atomically.

Suggested command shape:

```bash
ahm task comment <id> <text>
ahm task comment <id> --body-file -
ahm task comment <id> --author "Name" <text>
```

The implementation should start narrow:

- Store comments in the task Markdown body, not in a separate database or generated index.
- Use a predictable section such as `## Comments` or `## Activity`.
- Append newest comments after existing comments unless the task workflow chooses reverse chronological order.
- Include timestamp and optional author metadata in a stable Markdown format.
- Support `--body-file -` for multiline comments if that matches existing body-file patterns.
- Keep comments available on completed and cancelled tasks.

Do not introduce a separate event log, git commits, orphan branch storage, or generated comment index unless the implementation task explicitly expands scope.

## Design Questions

- Choose `## Comments` versus `## Activity`. `Comments` is simpler and matches the command name; `Activity` could later include lifecycle events.
- Decide whether author defaults to git `user.name`, the current OS user, or is omitted unless `--author` is provided.
- Decide whether comment appends should regenerate task indexes. If only body content and `updated` changes, index regeneration may still be needed because generated indexes display `updated`-sensitive task metadata.
- Decide whether JSON/plain output should return the appended comment record or only the task ID and path.

## Acceptance Notes

- [ ] `ahm task comment <id> <text>` appends a timestamped comment to the target task.
- [ ] Multiline comments are supported or explicitly rejected with documented behavior.
- [ ] Existing front matter, unknown fields, task status, bucket location, and body content are preserved.
- [ ] Comments work for active, completed, and cancelled tasks.
- [ ] The command uses atomic writes and respects `--dry-run` without mutating disk.
- [ ] Text, JSON, and plain output behavior is tested and documented.
- [ ] CLI docs and task file/workflow format docs describe where comments live and how they are rendered.

