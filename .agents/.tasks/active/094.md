---
id: 094
title: Rename deslop review workflow to a professional name
status: Pending
priority: P2
effort: S
labels: type:maintenance, area:agent, area:workflow
exec_plan: -
depends_on: -
created: 2026-06-12T20:47:48-04:00
---
# Rename deslop review workflow to a professional name

## Problem

The review-readiness workflow is currently named `deslop`, which is too informal for project documentation, agent instructions, and CLI-facing workflow references. The underlying idea is useful, but the name makes the workflow feel like a joke in places where it should read as maintained tooling.

## Relevant Files

- `.agents/skills/deslop/`
- `.agents/skills/deslop/SKILL.md`
- `.agents/TASKS.md`
- Agent and task workflow docs that reference `deslop`
- Task 091, if it still references the current name
- Any tests or fixtures that mention the skill by name

## Fix Direction

Rename the workflow from `deslop` to `preflight`. The name describes the systematic self-review pass before handoff — analogous to a preflight checklist. One rename, no alias or migration wrapper needed.

Changes required:
1. Rename `.agents/skills/deslop/` → `.agents/skills/preflight/` and update `SKILL.md` front matter (`name:` field, `description:`).
2. Update all references to `deslop` in workflow docs (`.agents/TASKS.md`, agent instructions, docs).
3. Update the embedded skill definition in `internal/templates/` if the skill is referenced from templates.
4. Update any task files that mention `deslop` in their body or notes.
5. Update `docs/cli.md` and test fixtures if they reference `deslop`.
6. Run `just ci` after the rename to verify nothing is broken.

## Acceptance Notes

- [ ] `.agents/skills/preflight/` exists with correct `name:` and `description:` in `SKILL.md`.
- [ ] No remaining references to `deslop` in workflow docs, agent instructions, or templates.
- [ ] Any tests or fixtures that reference the skill use `preflight`.
- [ ] `just ci` passes.

