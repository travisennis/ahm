---
id: 153
title: Update ARCHITECTURE.md module map for task_comment.go, agents.go, path.go
status: Open
priority: P3
effort: XS
labels: type:task,area:docs
exec_plan: -
depends_on: -
created: 2026-07-01T17:08:35-04:00
---
# Update ARCHITECTURE.md module map for task_comment.go, agents.go, path.go

## Summary

The ARCHITECTURE.md module map has drifted from the source tree: `internal/ahm/task_comment.go` (task comment command), `internal/ahm/agents.go` (agents suggestions command), and `internal/ahm/path.go` (relPath helper) exist but are not listed. Agents route work using this map (AGENTS.md workflow routing), so omissions misdirect them. Repo precedent treats module-map drift as fix-worthy (completed task 071). Written against commit ff15056.

## Evidence

- `ARCHITECTURE.md:33-79` — the module map lists every other non-test `internal/ahm/*.go` file but omits the three above.
- `internal/ahm/task_comment.go`, `internal/ahm/agents.go`, `internal/ahm/path.go` — present at HEAD.

## Fix Direction

Add one-line entries in the module map's existing style and grouping: task_comment.go near the other task_* entries; agents.go near cli.go/context.go; path.go near output.go. While there, compare `ls internal/ahm/*.go` (excluding _test.go) against the map and add any other missing file. Documentation-only change; no code edits.

## Acceptance Notes

- [ ] Every non-test Go file under `internal/ahm/`, `internal/templates/`, `internal/version/`, and `cmd/ahm/` appears in the ARCHITECTURE.md module map.
- [ ] `just docs-md-lint` passes on the edited file (or state why it was skipped).

