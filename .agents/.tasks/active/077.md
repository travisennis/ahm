---
id: 077
title: Add ahm adr list and show commands
status: Open
priority: P2
effort: S
labels: type:feature, area:adr, area:cli
exec_plan: -
depends_on: 075
created: 2026-06-11T21:25:23-04:00
updated: 2026-06-11T21:26:34-04:00
---
# Add ahm adr list and show commands

## Summary

Add `ahm adr list` and `ahm adr show <id>` mirroring `ahm task list` and
`ahm task show`.

## Background

Design per ADR 009 (task 074) and
`.agents/.research/topics/madr-adr-management-design.md`. Model and ID
resolution come from task 075.

## Implementation Notes

- `adr list` prints ID, title, status, and date, sorted by ID; supports
  `--status <value>` filtering. Superseded records should be filterable even
  though the status value embeds the replacement ID (e.g. match on the
  `superseded` prefix); decide exact matching semantics per ADR 009.
- `adr show <id>` prints one record using `resolveADR` (`9`, `009`, or
  `009-slug`).
- Support `--json`, `--plain`, and `--text` output modes consistently with
  the task commands (`internal/ahm/output.go`).
- Legacy or malformed files must not abort either command; report them the
  way malformed tasks are reported (task 033 behavior).
- Update `docs/cli.md` with `adr list` and `adr show` sections in the same
  change.

## Relevant Files

- `internal/ahm/task_commands.go` (pattern to mirror)
- `internal/ahm/output.go`
- `docs/cli.md`

## Acceptance Notes

- [ ] `adr list` and `adr show` work against this repo's `docs/adr/` and
      tolerate legacy files.
- [ ] Status filtering covers fixed statuses and superseded records.
- [ ] Output mode tests cover text, plain, and JSON.
- [ ] `docs/cli.md` documents both commands; `just ci` passes.

