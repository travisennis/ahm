---
id: 135
title: cleanupStaleTemps stops WalkDir on first os.Remove failure
status: Open
priority: P2
effort: S
labels: type:bug,area:workflow
exec_plan: -
depends_on: -
created: 2026-06-26T17:41:35-04:00
---
# cleanupStaleTemps stops WalkDir on first os.Remove failure

## Summary

`cleanupStaleTemps` in `internal/ahm/write.go` walks `.agents/` looking for orphaned `.tmp` files. When `os.Remove` of a `.tmp` file returns a non-nil error (permission denied, concurrent removal by another process), the WalkDir callback returns that error, causing `filepath.WalkDir` to stop immediately. Remaining stale `.tmp` files are silently skipped since the callsite at `cli.go:226` discards the return value (`_ = cleanupStaleTemps(...)`).

## Evidence

- `internal/ahm/write.go:148` — `return os.Remove(cleanPath)` inside WalkDir callback returns the os.Remove error, terminating the walk on any failure.
- `internal/ahm/cli.go:226` — `_ = cleanupStaleTemps(a.opts.root)` discards the error, so the premature stop is invisible to the user.

## Fix Direction

In the WalkDir callback, treat `os.Remove` errors as non-fatal: when the error is `os.ErrNotExist` (concurrent removal by another process), continue walking silently. For other errors, return `nil` to continue the walk rather than stopping it. After the walk completes, if any removal errors occurred, surface them via `a.addWarning` so the user knows stale temps weren't fully cleaned.

Do NOT change the callsite at `cli.go:226` — the function signature stays the same; the fix is entirely inside `cleanupStaleTemps`.

## Acceptance Notes

- [ ] `just test ./internal/ahm` passes. Write a test in `write_test.go` that verifies WalkDir continues past a failed `os.Remove` (mock by making a read-only temp file and verifying subsequent .tmp files are still processed).
- [ ] Manually verify: create a read-only `.tmp` file under `.agents/`, run `ahm index`, confirm other stale `.tmp` files are still cleaned up.
- [ ] Existing `write_test.go` tests pass unchanged.

