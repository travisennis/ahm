---
id: 049
title: Document ahm task create in TASKS.md workflow
status: Pending
priority: P2
effort: S
labels: type:bug, area:workflow, area:docs
exec_plan: -
depends_on: -
---
# Document ahm task create in TASKS.md workflow

## Summary

`.agents/TASKS.md` and its embedded template
`internal/templates/workflow/TASKS.md` describe creating tasks only through
the manual workflow: writing a Markdown file in `.agents/.tasks/active/` and
then running `ahm index`. The `ahm task create` command already exists, is
documented in `docs/cli.md`, and is the preferred CLI-driven method, but
neither TASKS.md document mentions it. The `--body-file` flag also exists so
agents can create a complete task body in one command, but that purpose is not
clear enough from the current workflow guidance or terse command help.

## Problem

The "Creating Tasks" section of `.agents/TASKS.md` opens with:

> Create new tasks in `.agents/.tasks/active/` with the next available
> three-digit id...

The "Updating Tasks" section lists `ahm task start`, `ahm task complete`,
and `ahm task cancel` as the managed commands but omits `ahm task create`.
Readers who follow the documented workflow exclusively will create tasks by
hand, which is more error-prone, misses the ID-allocation and index-regeneration
logic that `ahm task create` handles automatically, and produces less
consistent task bodies.

Even when agents discover `ahm task create`, they may still create a placeholder
task and patch the body afterward because the documentation does not strongly
position `--body-file` as the intended path for complete task records with
Problem, Relevant Files, Fix Direction, and Acceptance Notes sections. That
weak affordance undercuts the main value of `--body-file`: letting `ahm` own ID
allocation, front matter, placement, and index regeneration while the caller
supplies the full Markdown body.

## Relevant Files

- `.agents/TASKS.md` — the local workflow document consumed by agents.
- `internal/templates/workflow/TASKS.md` — the embedded template from which
  `.agents/TASKS.md` is created/upgraded.
- `internal/ahm/cli.go` — the `taskCreateParsed` handler and the task-create
  command wiring.
- `internal/ahm/task_commands.go` — the `--body-file` flag and task-create
  body handling.
- `docs/cli.md` — the user-facing CLI reference (already documents the
  existing `ahm task create` behavior).

## Fix Direction

Update both `.agents/TASKS.md` and `internal/templates/workflow/TASKS.md` so
the "Creating Tasks" section:

1. Introduces `ahm task create <title> [flags]` as the preferred creation
   path.
2. Notes the flags available (`--priority`, `--effort`, `--labels`, `--status`,
   `--description`, `--body-file`) or points to `docs/cli.md` for the full
   reference.
3. Keeps the manual creation instructions as the fallback or detail reference
   (for agents that cannot run `ahm` or want full control over the body).
4. States that `ahm task create` regenerates indexes automatically, so no
   separate `ahm index` is needed after creation.
5. Explicitly recommends `--body-file` when creating a complete task record
   with the standard sections, so agents do not create a placeholder task and
   patch it afterward.

Also check whether the "Do not run `ahm index` after..." paragraph near the top
of TASKS.md needs updating. Today it lists only `start`, `complete`, and
`cancel`; `create` also regenerates indexes.

Also consider sharpening the `ahm task create --help` text for `--body-file`.
The current help is accurate but does not make the primary use case obvious.
If changed, keep it concise, update `docs/cli.md` only if the command contract
changes, and add focused CLI-help coverage if the project already tests help
text in this area.

Scope: primarily docs-only changes to TASKS.md and the embedded template. A
small CLI help-string change is acceptable if it directly improves the
`--body-file` affordance. Because the embedded template is a managed file
source, `go test ./internal/templates` should be run after the template edit to
verify it still embeds correctly. If Go code changes, follow the normal Go
verification path before `just ci`.

If the task workflow documentation is considered a durable workflow contract,
create or update an ADR before implementation. That is unlikely for this
change, but note the question in the acceptance notes.

## Acceptance Notes

- [ ] `.agents/TASKS.md` "Creating Tasks" section introduces `ahm task create`
      as the primary creation method before the manual workflow instructions.
- [ ] `.agents/TASKS.md` notes that `ahm task create` regenerates indexes
      automatically.
- [ ] `.agents/TASKS.md` explains that `--body-file` is the preferred way to
      create a complete task body without a follow-up patch.
- [ ] `internal/templates/workflow/TASKS.md` is updated to match.
- [ ] The "Do not run `ahm index` after..." paragraph lists `create` alongside
      `start`, `complete`, and `cancel`, or the paragraph is restructured to
      cover all `ahm task ...` commands that regenerate indexes.
- [ ] The `ahm task create --help` text for `--body-file` is reviewed and
      sharpened if the current wording still hides the intended use.
- [ ] `go test ./internal/templates` passes after the template change.
- [ ] No broad CLI behavior changes are made; any Go change is limited to help
      text and directly related tests.
- [ ] `just ci` is green before handoff.
