---
id: 263e
title: Run CI on all pushes and guard release prep to master
status: In Progress
priority: P1
effort: S
labels: type:task, area:devx
exec_plan: -
depends_on: -
created: 2026-08-01T19:50:52-04:00
updated: 2026-08-01T20:52:44-04:00
parent: 263
---
# Run CI on all pushes and guard release prep to master

## Summary

Milestone 1: change .github/workflows/ci.yml so CI runs on every push (not just master) so feature branches are verified before PR; add a master guard to scripts/prepare-release.sh so release prep cannot run from a feature branch (it currently pushes $current_branch).

## Acceptance Notes

- [ ] Pushing a feature branch to GitHub triggers the `ci` workflow.
- [ ] `scripts/prepare-release.sh` on a feature branch exits non-zero with the
      master-only message (verified: exit 1 on `feat/263e-ci-releases`).
- [ ] `scripts/prepare-release.sh` on master passes the guard, regenerates
      `CHANGELOG.md`, and prints the release-branch + PR instructions
      (verified in a throwaway clone; full run deferred to next release).
- [x] `docs/release.md` describes the release-branch + PR flow and no longer
      instructs committing the changelog on master or `git push origin master`.
- [x] `just ci` passes locally.

