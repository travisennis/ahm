---
id: 264a
title: Delete task work, audit, and groom with their shared delegation plumbing
status: Pending
priority: P1
effort: M
labels: type:workflow, area:devx
exec_plan: -
depends_on: -
created: 2026-09-20T07:40:26-04:00
parent: 264
---
# Delete task work, audit, and groom with their shared delegation plumbing

## Summary

Remove the delegation surface. `ahm task work`, `ahm audit`, and
`ahm task groom` share `task_agents.go`, `task_session.go`, and
`task_parsers.go`, so they are removed as one unit, together with their tests,
`internal/ahm/testdata/agents`, the `smoke-agents` and
`capture-agent-fixtures` recipes, `scripts/capture-agent-fixtures.sh`, and
`docs/guardrails/external-agent-orchestration.md`. Follow-through: drop the
`taskWork` config seed and its validation, and any task-work finding codes.

## Acceptance Notes

- [ ] `ahm task work`, `ahm audit`, and `ahm task groom` fail as unknown
      commands, and `ahm task --help` no longer lists them.
- [ ] No code, recipe, script, or guardrail references agent definitions,
      argument builders, session capture or resume, JSONL parsing, golden
      transcripts, or provider smoke tests.
- [ ] No test makes a network call and `AHM_AGENT_SMOKE` no longer selects
      any test path.
- [ ] `ahm doctor` and `ahm status` report no task-work or grooming findings.
- [ ] `just ci` passes.

