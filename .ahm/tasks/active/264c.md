---
id: 264c
title: Remove the procedure channel and the last prescription
status: Pending
priority: P1
effort: S
labels: type:workflow, area:devx
exec_plan: -
depends_on: -
created: 2026-09-20T07:40:26-04:00
parent: 264
---
# Remove the procedure channel and the last prescription

## Summary

Delete `context.go` and `onboard.go` with the `context` and `onboard`
commands, prime's managed-work routing, `Managed Work Intake`, grooming and
audit hints, and the backlog-routing prose, plus status's onboarding snippet.
Before deleting them, salvage what the command prints: `ahm context task`
becomes `docs/workflow/tasks.md`, `ahm context plan` becomes
`docs/workflow/exec-plans.md`, and `ahm context adr` becomes
`docs/workflow/adrs.md`. These are hand-maintained project documents after the
change; the research procedure is dropped with its record family.

## Acceptance Notes

- [ ] `ahm context` and `ahm onboard` fail as unknown commands.
- [ ] `ahm prime` regenerates indexes, reports validation findings and record
      counts, and contains no text that names a command to run or a step in a
      workflow.
- [ ] No file in `AGENTS.md`, `CONTRIBUTING.md`, or `docs/` instructs a reader
      to run a removed command.
- [ ] `docs/workflow/tasks.md`, `docs/workflow/exec-plans.md`, and
      `docs/workflow/adrs.md` exist, contain no `{{` template variables, name
      no removed command, and are linked from `docs/README.md`.
- [ ] The plan document points at `docs/exec-plans/` rather than the retired
      `.ahm/exec-plans/`, and `docs/exec-plans/active/` and
      `docs/exec-plans/completed/` exist.
- [ ] Task and ADR procedure lives in project-owned prose, and no ahm command
      emits workflow instructions.
- [ ] `just docs-md-lint` and `just ci` pass.

