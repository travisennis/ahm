---
id: 163
title: Record sessions for every task work agent run
status: Blocked
priority: P2
effort: M
labels: type:task, area:agent, risk:external-service
exec_plan: -
depends_on: -
created: 2026-07-07T13:42:47-04:00
updated: 2026-07-20T09:39:53-04:00
---
# Record sessions for every task work agent run

## Blocker

Implementation and hermetic verification are complete. Completion is blocked
only on fresh live smoke evidence for the supported providers: Claude requires
available account credit and Cursor requires its usage limit to reset. Rerun
the documented smoke commands when those external capabilities are available;
no additional source change is currently required.

## Problem

`ahm task work` should produce a resumable, auditable session for every external agent invocation it starts. At least one cake path currently passes `--no-session`, which prevents session creation and undermines auditability for delegated work.

The goal is to remove session-suppression from task-work agent invocations and make session capture a required invariant across supported session-capable agents.

## Relevant Files

- `internal/ahm/task_agents.go` - agent definitions and argument builders; the cake invocation currently includes `--no-session`.
- `internal/ahm/task_parsers.go` - session ID parsers and resume argument helpers for supported agents.
- `internal/ahm/task_session.go` - orchestration/session persistence path for `ahm task work`.
- `internal/ahm/task_work_smoke_test.go` - live smoke coverage for session capture and resume behavior.
- `internal/ahm/testdata/agents/` - golden transcripts and provenance sidecars if any agent output schema changes.
- `docs/guides/testing.md` - live agent smoke checklist and required evidence.

## Fix Direction

- Audit every `ahm task work` agent command path, including work, review, completion, and commit handoff where relevant, for flags or modes that suppress session creation.
- Remove the cake `--no-session` usage from task-work invocations and ensure cake still emits stream JSON with a capturable session ID.
- Add or update focused tests so session-capable agent definitions cannot silently opt out of session creation.
- Keep resume behavior consistent: the captured session ID should be used for follow-up prompts, review, completion, and commit handoff wherever the orchestration expects a session.
- Refresh golden transcripts only if the real CLI output schema changes.

## Acceptance Notes

- [x] No `ahm task work` agent invocation suppresses session creation for a session-capable agent.
- [x] Cake task-work runs create and report a session ID instead of running with `--no-session`.
- [x] Focused unit/parser/orchestration tests cover the no-session regression risk.
- [x] `just ci` passes.
- [ ] Live agent smoke evidence is recorded before completion per `docs/guides/testing.md`, including installed agent versions and transcript snippets showing `session started:` without `no session ID returned by` or `could not capture session ID`.

## Live Smoke Evidence

Installed versions on 2026-07-07:

- `cake`: `cake 0.1.0`
- `claude`: `2.1.202 (Claude Code)`
- `codex`: `codex-cli 0.142.5`
- `cursor-agent`: `2026.07.01-41b2de7`

Commands:

- `just smoke-agents` passed `cake`, `codex`, and `cursor`, but failed overall because `claude` returned `Credit balance is too low`.
- `AHM_AGENT_SMOKE=1 go test ./internal/ahm -run 'TestAgentSmoke/(cake|codex|cursor)$' -v -count=1 -timeout 30m` captured evidence for usable agents, but failed overall because `cursor-agent` hit its usage limit during commit handoff.

Transcript snippets:

```text
cake session started: 5558edf9
codex session started: 019f3dc8
cursor session started: 33c847e2
```

Negative evidence:

- No `no session ID returned by` or `could not capture session ID` messages appeared in the captured cake, codex, or cursor evidence.
- Full live smoke is still blocked by external account state: Claude billing and Cursor usage limit.

## Comments

**2026-07-07T14:24:45-04:00** - Implementation and CI are complete. Full live smoke is blocked by external account limits: Claude returned 'Credit balance is too low', and cursor-agent later hit its usage limit during commit handoff. Cake, Codex, and Cursor all emitted session started evidence before the blocker.

**2026-07-20T09:39:53-04:00** — Implementation and hermetic CI complete. Remaining work is solely external: Claude credit and Cursor usage-limit reset for live smoke evidence. No stale dependencies (depends_on is empty by design). The task is well-documented with evidence of captured sessions for cake, codex, and cursor.
