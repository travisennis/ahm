---
id: 228
title: Warn when ADR parse errors are skipped during index generation
status: Completed
priority: P2
effort: XS
labels: type:bug, area:workflow
exec_plan: -
depends_on: -
created: 2026-07-19T21:46:04-04:00
updated: 2026-07-22T21:12:22-04:00
---
# Warn when ADR parse errors are skipped during index generation

## Problem

`indexWritesForPaths` ignores the error from `collectADRs` whenever any ADR parsed (`if err != nil && len(adrs) == 0`), with no warning — unlike the task path, which warns about skipped unparseable files. Because `renderADRIndex` skips malformed ADRs, `ahm index` can silently drop entries from docs/adr/index.md.

## Relevant Files

- `internal/ahm/indexes.go` (indexWritesForPaths, renderADRIndex)
- `internal/ahm/adrs.go` (collectADRs)

## Fix Direction

Surface the partial-parse error as a warning on the same channel the task path uses ("some ADR files could not be parsed and were skipped: ..."), threading a warning sink or returning the error alongside the writes so app-level callers can report it.

## Acceptance Notes

- [x] Index generation with a malformed ADR present emits a warning naming the file
- [x] Test covers the partial-parse warning path

## Comments

**2026-07-20T09:39:53-04:00** — Ready. Clear bug (silent ADR parse-error skip during index generation, unlike the warning-emitting task path). Fix direction is precise and acceptance notes are minimal and verifiable. Priority P2 and effort XS are appropriate.

**2026-07-22T21:12:12-04:00** — Preflight verified the partial ADR parse warning and filename in focused tests. Validation continues checking indexes rendered from readable ADRs without duplicating the existing `adr_malformed` finding. `just ci` gates passed with caches redirected to `/tmp` where required by the sandbox.

