---
id: 079
title: Generate ADR index and validate ADR records
status: Completed
priority: P2
effort: M
labels: type:feature, area:adr, area:workflow, area:cli
exec_plan: .agents/exec-plans/completed/074-madr-adr-management.md
depends_on: 075
created: 2026-06-11T21:25:52-04:00
updated: 2026-06-14T10:31:24-04:00
---
# Generate ADR index and validate ADR records

## Blocker

Blocked on task 075 for ADR collection/parsing. ADR 009 records the
validation-scope and legacy-severity decisions.

## Summary

Generate `docs/adr/index.md` from ADR records via `ahm index` and add ADR
validation findings to `ahm status` / `ahm doctor`.

## Background

Design per ADR 009 (task 074) and
`.agents/.research/topics/madr-adr-management-design.md`. This changes
generated index semantics, so it is covered by the feature ExecPlan from task
074.

## Implementation Notes

- Add the ADR index to `indexWritesFor` and `generatedIndexTargets` in
  `internal/ahm/indexes.go`: a deterministic table (ADR, Title, Status, Date)
  sorted by ID, with the standard "generated by `ahm index`, do not edit"
  banner and `escapeCell` escaping. `README.md` and `index.md` are excluded
  from collection.
- Dry-run reports the ADR index only when stale, matching tasks 045 and 064.
- Validation findings: ADR parse failures, invalid status values (fixed set
  plus `superseded by ADR-NNN` pattern), ID/filename mismatches, duplicate
  IDs, supersede references that do not resolve, and a stale generated ADR
  index. Legacy-format files surface as a distinct finding pointing at
  `ahm adr migrate`.
- Per ADR 009, fold the checks into the existing `workflow` scope. Legacy-file
  findings must not make self-hosted `ahm status` unusable before task 081
  migrates this repo's ADRs.
- Update `docs/spec.md` (workflow state, file ownership boundary, validation
  scopes/findings) and `docs/cli.md` (`index`, `status`, `doctor`, validation
  findings sections) in the same change.

## Relevant Files

- `internal/ahm/indexes.go`
- `internal/ahm/validation.go`
- `internal/ahm/status.go`
- `docs/spec.md`
- `docs/cli.md`

## Acceptance Notes

- [x] `ahm index` produces deterministic `docs/adr/index.md`; focused tests
      cover rendering and stale-only dry-run behavior, and
      `go run ./cmd/ahm --dry-run index` is quiet after regeneration.
- [x] `status`/`doctor` report ADR validation findings. Tests cover
      `adr_malformed`, `adr_invalid_status`, `adr_id_mismatch`,
      `adr_duplicate_id`, `adr_supersede_missing`, and `adr_legacy_format`.
- [x] Legacy-format findings do not make this repo's own `ahm status`
      unusable before task 081 lands; `go run ./cmd/ahm status` exits 0 with
      legacy ADR warnings.
- [x] `docs/spec.md` and `docs/cli.md` updated; `just ci` passes.

