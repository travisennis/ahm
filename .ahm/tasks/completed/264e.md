---
id: 264e
title: Rewrite documentation and instructions to the records boundary
status: Completed
priority: P1
effort: M
labels: type:workflow, area:devx
exec_plan: -
depends_on: 264a, 264b, 264c, 264d
created: 2026-09-20T07:40:31-04:00
updated: 2026-09-20T20:07:30-04:00
parent: 264
---
# Rewrite documentation and instructions to the records boundary

## Summary

Bring every prose surface in line with ADR 022: `docs/VISION.md`,
`README.md`, `docs/README.md`, `docs/cli.md`, `docs/references/cli/*`,
`docs/references/workflow-spec.md`, `docs/references/glossary.md`,
`docs/guardrails/*`, `docs/guides/*`, `AGENTS.md`, `CONTRIBUTING.md`, and
`ARCHITECTURE.md`. VISION's four channels and four record families are
replaced by the records-CLI boundary; the routing rules in `AGENTS.md` and
`CONTRIBUTING.md` that pointed at removed commands are replaced with the
procedure the project now owns, and the new `docs/workflow/` and
`docs/exec-plans/` surfaces are registered in the documentation map.

## Acceptance Notes

- [x] Every live document describes the reduced tool: none instructs a reader
      to run a removed command or manage a removed record family, per the prose
      rule for removals in the ExecPlan.
- [x] `docs/VISION.md` states the two-family boundary and the accepted
      tradeoff that procedure prose is project-owned and may drift.
- [x] `ARCHITECTURE.md`'s compatibility surfaces, system boundaries, and
      module map match the reduced tree.
- [x] `AGENTS.md` still routes task and ADR work, names `docs/exec-plans/` as
      where design plans live, and the operating loop it describes is
      executable with the remaining commands only.
- [x] `docs/README.md` and the ownership table in
      `docs/guardrails/documentation.md` list `docs/workflow/` and
      `docs/exec-plans/`.
- [x] `just docs-md-lint` and `just ci` pass.

## Comments

**2026-09-20T20:07:15-04:00** — Milestone 6 pass: every live prose surface was rewritten to the two-family records boundary (README, ARCHITECTURE, CONTRIBUTING, .agents/prompt.md, the two skills, and the live docs under docs/). Two subagent review rounds ran; all nine round-one findings were fixed, and round two escalated one release-naming question to 264f: root.go names v1.0.0 as the release to upgrade a legacy .agents/ahm.json repository with, but that tag predates ahm records migrate, so it cannot perform the migration. The dated pre-v2 release history in docs/guides/workflow-upgrades.md was deleted rather than rewritten (prose rule for removals); the plan's Decision Log records why. just ci and just docs-md-lint are green. Nothing is committed: the change is 17 modified files in the worktree.

