---
id: 267f
title: Default new projects to the home store
status: Completed
priority: P1
effort: S
labels: type:feature, area:config
depends_on: 267e
created: 2026-09-21T11:29:33-04:00
updated: 2026-09-22T16:36:52-04:00
parent: 267
---
# Default new projects to the home store

## Summary

Turn on the new default. `install()` in `internal/ahm/install.go` writes
`tasks_location: home` when it creates configuration for a repository that has
none, and preserves an existing value otherwise. A repository that already has
`.ahm/config.json` without the key stays in `project` mode, which is what makes
this work safe to land in the existing tree.

Root detection is unchanged. A directory holding `.git` or `.ahm/config.json`
is a managed root; a directory with neither still needs `ahm init`, which then
writes configuration with `home` as the mode and, because there is no remote,
keys the project by its symlink-resolved path.

Update `ahm init`'s help text and the CLI reference to say what the command now
creates and where records will live, and note that `--tasks-project` is not a
flag: a person who wants records in the project in a new repository runs
`ahm store migrate --to project` after `ahm init`.

## Acceptance

- `git init` plus `ahm init` in a fresh directory creates configuration with
  `tasks_location: home` and puts a created task in the store.
- A repository whose configuration predates this change keeps its records in
  the project and creates no store entry.
- `ahm init` on a `home` repository never creates `.ahm/tasks/`, including
  when run twice and when run with `--force`.
- A non-Git directory that has run `ahm init` works, keyed by its path.
- `just ci` passes.

## Comments

**2026-09-21T14:36:33-04:00** — 267c handed two things to this milestone. First, `resolveTaskLocation`'s no-configuration branch still returns `locationProject`; flipping it to `locationHome` is the default turn-on. Second, `install` resolves its own layout from the metadata it is about to write (`resolveWorkflowPaths(root, meta, configExists)` and `app.useWorkflowPaths`), so set `meta.TasksLocation` before resolving and the same run lays down the store directories. Write the key only when the configuration is being created; preserve it otherwise.

**2026-09-22T16:36:43-04:00** — Implemented and reviewed (2026-09-22). resolveTaskLocation now resolves a repository with no .ahm/config.json as home, and install writes tasks_location: home into the configuration it creates, records the new project in the store registry, reconciles the managed .gitignore of every layout the mode owns from one workflowPaths.managedGitignores list (superseding 267e's decision that install owns only the store's file in home mode), and warns when the mode it writes would strand records still in the project. An uninstalled repository no longer reports a store block or store_dir_unreadable, and store migrate's no-work shortcut now requires a committed configuration, so a repository that names no layout is set up in the mode --to asks for. The init help text, the CLI reference (commands.md, global-contract.md), ARCHITECTURE.md, and the safety guardrail were updated; the README, workflow spec, upgrade guide, glossary, and the root command's repo-local summary stay with 267h. Verified by hand in scratch repositories (fresh git init plus init, repeated and --force init, an existing configuration without the key, a non-Git directory, --dry-run init writing nothing, store migrate --to project on a repository with no configuration) and by just ci (fmt-check, tidy-check, vet, race tests, golangci-lint, govulncheck, markdownlint, goreleaser snapshot), with ahm doctor reporting no findings in this repository. Two review rounds and a preflight pass, each with mutation-probed tests, are recorded in the exec plan's 2026-09-22 entries.

