---
id: 036
title: Read each task file once during validation
status: Completed
priority: P2
effort: S
labels: type:perf, area:tasks
exec_plan: -
depends_on: -
updated: 2026-06-03T21:10:27-04:00
---
# Read each task file once during validation

## Summary

Review issue 36. validateTaskFiles reads each task twice via validateTaskFrontMatter and parseTask. Acceptance: validation performs one read per task file and reuses parsed data, with validation tests passing.

## Acceptance Notes

- [x] validateTaskFiles reads each task file exactly once: the file is read by `readWorkflowFile` in the loop body, then the data is passed to `validateTaskFrontMatter` (for front-matter validation) and `parseTaskFromData` (for full task parsing).
- [x] All validation tests pass (TestValidateTaskFrontMatter_CRLF, TestValidateTaskFrontMatterReportsParseErrors, and all status/doctor integration tests).
- [x] `parseTask` still works for callers that need it (collectTasks, tests) by delegating to `parseTaskFromData` after reading.

