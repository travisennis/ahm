---
id: 267c
title: Read and write task records in the home store
status: Completed
priority: P1
effort: L
labels: type:feature, area:tasks
depends_on: 267b
created: 2026-09-21T11:29:26-04:00
updated: 2026-09-21T15:36:19-04:00
parent: 267
---
# Read and write task records in the home store

## Summary

Make the store real: task commands read and write records under the records
root, indexes split across the two roots, `prime` and `status` report the
store, and validation reports drift when the mode and the records disagree.
The default mode stays `project` until 267f, so this milestone changes behavior
only for a configuration that already names `home`.

Configuration: add `TasksLocation` to `metadata` in `internal/ahm/install.go`,
in both the hand-written `MarshalJSON` and the `UnmarshalJSON` delete list so
it is never preserved as an unknown field. Add

    func resolveTaskLocation(meta metadata, configExists bool) taskLocation

A missing key means `project`; a repository with no configuration is `home`,
which 267f turns on. Wire `resolveWorkflowPaths` into `app` after root
detection.

Install: when the mode is `home`, `install()` creates the store project
directory (`tasks/{active,completed,cancelled}`, `.lock`, and the store's
managed `.gitignore`) and never creates `.ahm/tasks/`. Reuse the existing
exists-but-not-a-directory error from `ensureWorkflowDirs`.

Indexes: in `indexWritesForPaths`, the four task indexes are written under the
records root and `docs/adr/index.md` stays under the project root. Keep index
content byte-identical; a task index link such as `active/001.md` must keep
resolving because the store mirrors the in-project layout.

Validation: `workflowMarkdownFilesForPaths` already enumerates both families
from resolved paths, so it should follow the records root. Add a read-only
finding, `task_records_in_project`, reported when the mode is `home` and task
records still exist under `.ahm/tasks/`, and a finding for a `home` project
whose store directory is missing or unreadable. Update
`validateMarkdownFileLinks` to resolve a target against the record's own
directory first and, only if that does not exist, against the record's logical
in-project directory, per ADR 023.

Output: `prime` and `status` report `store: {root, key, kind, location}` and
display store paths with the `store:` prefix. Keep `project` mode output
byte-identical. 267b left the remaining path *display* on `relPath(root, …)`:
validation findings, `validateTaskBuckets` messages, the `collectTasksForPaths`
parse-error message, `Task.Path` in JSON, the `checkDuplicateTaskID` and
`checkTaskDepsNotDuplicated` messages, the dry-run `move` and unblock preview
payloads in `task_status.go`, and lock-timeout errors all relativize a record
path against the project root. Switch them to `displayPath` in this milestone,
so a record path is never rendered against the wrong root, and make
`displayPath` the single label helper for record paths. Confirm the delete side
too: `task_status.go` removes a vacated record path directly with `os.Remove`,
with no owned-root check.

Store `.gitignore`: ADR 023 and the ExecPlan's Idempotence section say the
store root holds the managed `.gitignore` that ignores `registry.json`, while
the ExecPlan's Artifacts layout puts it at `<store>/projects/<slug>-<hash>/`.
267b's `ownedRoots` is the project root plus the store's project directory, so
a store-root write is refused. Decide where the file goes here — placing it per
project needs no change, while ignoring `registry.json` needs the store root
admitted as an owned root — and hand the ADR wording to 267h.

## Acceptance

- With `tasks_location: home`, `ahm task create`, `list`, `show`, `complete`,
  and `dep` operate entirely in the store and write nothing under
  `.ahm/tasks/`.
- With the key absent, the entire existing suite passes with byte-identical
  output and layout; this is the guard for existing repositories and must not
  be weakened.
- `ahm doctor` reports `task_records_in_project` when a record is left in the
  project while the mode is `home`.
- A task record whose body links to `../../../docs/adr/<n>-<slug>.md` still
  validates after a move, and a link to a sibling task still validates.
- `just ci` passes.

## Comments

**2026-09-21T15:36:03-04:00** — 267c landed. `metadata` gained `TasksLocation` (`tasks_location` in `.ahm/config.json`), written by the hand-written `MarshalJSON` and consumed from the unknown-field map on read, plus `resolveTaskLocation(meta, configExists)` and `resolveWorkflowPaths(root, meta, configExists)`. `app` resolves the records layout once after root detection and caches it, `workflowPaths()` is the lazy accessor for helpers that cannot return an error, and `install` re-resolves the layout from the configuration it owns. Task reads and writes, the four task indexes, the lock, and the managed `.gitignore` all follow the records root; `.ahm/config.json` and `docs/adr/` stay in the project. `prime` and `status` report `store: {root, key, kind, location}` only when the records live in the store. Validation gained `task_records_in_project` and `store_dir_unreadable`, and Markdown link validation resolves a relative link against the record's own directory first and the record's logical in-project directory second.

Acceptance, against the milestone's own criteria. With `tasks_location: home`, `ahm task create`, `list`, `show`, `complete`, and `dep` operate entirely in the store and write nothing under `.ahm/tasks/`: `TestHomeModeKeepsTaskRecordsInTheStore` drives create, `list`, `show`, `--json list`, `--json search`, a dependent create, the dry-run complete with its `unblocked` preview, the real complete, the indexes, the store's `.gitignore`, and that `.ahm/` holds only `config.json`; `TestHomeModeStatusReportsTheStore` covers the `status` and `prime` store fields; `TestHomeModeFindingsRenderStorePaths` covers the `store:` labels for a bucket mismatch and an unreadable bucket. `ahm doctor` and `ahm status` report `task_records_in_project` for a record left in the project while the mode is `home` (`TestDoctorReportsTaskRecordsLeftInTheProject`, which also proves leftover generated indexes in the project are not drift) and `store_dir_unreadable` when the store records directory is missing (`TestValidationReportsAnUnreadableStoreRecordsDirectory`). A store record linking to `../../../docs/adr/001-probe.md` still validates after the move, a sibling link resolves from the record's own directory, and a broken link is still reported (`TestStoreRecordLinksResolveAgainstTheProjectLayout`). With the key absent the entire suite passes and project-mode output is byte-identical: `TestProjectModeOutputHasNoStoreField` pins the absent store field for `status`, `prime`, and `doctor` plus the absolute project record path in `task list --json`, `task show --json`, and the dry-run `move` preview, and `TestWorkflowPathsPayloadPathKeepsProjectPaths` pins `payloadPath` for both layouts. `just ci` passes.

Five judgment calls, all recorded in the ExecPlan's Decision Log and Surprises sections. First, `resolveTaskLocation` returns `project` when there is no configuration; resolving that case as `home` splits the records between the run that creates the configuration and every run after it (and fails 87 existing tests), so 267f flips the branch and writes the key in the same run — the seam is `install`'s `resolveWorkflowPaths` plus `app.useWorkflowPaths`. Second, `payloadPath` renders `store:<rel>` for a store path and leaves a project path untouched, because the JSON `path` field and the dry-run previews carry the record's absolute path today and ADR 023 keeps project-mode output byte-identical; 267b's handover list assumed those sites were already project-relative, which they are not. Third, the store's managed `.gitignore` sits in the store's project directory, ignores the generated indexes, `.lock/`, `*.tmp`, and `project.json`, and leaves the store root outside the owned roots, so `registry.json` stays commit-visible derived data; ADR 023 is silent on this and 267h owes the wording. Fourth, the drift findings are error-tier, read-only, and run in the disk-reading validation (`status`, `doctor`, `prime`) plus the fallback a mutation or `ahm index` takes on a partial parse, never in the clean post-mutation path. Fifth, the delete side of a record move stays a direct `os.Remove` of the scanned record path, confined by construction, and 267e owns any stricter check.

Verified: `just ci` green end to end (gofmt, `go mod tidy -diff`, vet, race tests at 89.1% coverage, golangci-lint 0 issues, govulncheck, markdownlint 0 issues across 64 files, GoReleaser snapshot build); `ahm doctor` reports no findings; a full project-mode command battery against the v1.0.0-era binary at 4e7a8fe is normalized-identical in stdout, stderr, exit codes, and on-disk layout. Three subagent review rounds were planned and two were run: the first found the absolute store path leaked by `ahm --json task list`, that the evidence cited for the `resolveTaskLocation` decision was wrong, three unprefixed store labels, and the missing `project.json` ignore entry; the second verified every fix by command and mutation probe and found two vacuous init-label assertions and two stale ignore lists, all fixed, and reported no blockers or majors remaining. 27 modified files and one new file (`internal/ahm/store_records_test.go`) in the worktree; nothing committed.

