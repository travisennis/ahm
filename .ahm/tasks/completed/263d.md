---
id: 263d
title: Refuse direct commits to master via pre-commit guard hook
status: Completed
priority: P2
effort: S
labels: type:task, area:devx
exec_plan: -
depends_on: -
created: 2026-08-01T19:50:52-04:00
updated: 2026-08-01T23:17:15-04:00
parent: 263
---
# Refuse direct commits to master via pre-commit guard hook

## Summary

Milestone 3: add scripts/hooks/require-feature-branch.sh wired into
.pre-commit-config.yaml that fails when git branch --show-current is master.
Deliberately NO bypass (no env var, no exceptions): everything lands on a
branch, and Milestone 1 moves release commits to a release branch, so no
legitimate master commit exists. Local nudge only; GitHub branch protection is
the real gate.

## Acceptance Notes

- [x] On master, git commit fails with the guard message (verified: staged the
      hook files on a clean master worktree and ran `git commit --allow-empty`;
      the require-feature-branch hook failed with the guard message and HEAD
      did not move; hook files removed afterward).
- [x] Reading the hook confirms there is no conditional on any environment
      variable and no bypass (the script only checks `git branch
      --show-current` and has no other branches or environment reads).
- [x] On feat/<slug>, git commit is unaffected (the hook files themselves were
      committed on feat/263d-master-guard with the guard passing).
- [x] prek run --all-files (or the equivalent invocation) passes (all hooks
      green, including require-feature-branch).
- [x] just ci passes.

