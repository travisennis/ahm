---
id: 093
title: Add label-focused task listing commands
status: Completed
priority: P2
effort: M
labels: type:feature, area:tasks, area:cli
exec_plan: -
depends_on: -
created: 2026-06-12T20:47:46-04:00
updated: 2026-06-13T14:05:57-04:00
---
# Add label-focused task listing commands

## Problem

Task labels exist, but the CLI needs a better API for using them directly. Current task selection is too status-first and requires users or agents to infer label capabilities from generated indexes or raw files.

The desired direction is a clearer label workflow: list tasks by label, identify labels that make work ready or routeable, and list the label vocabulary currently present in the repository.

## Relevant Files

- `internal/ahm/cli.go`
- `internal/ahm/task_commands.go`
- `internal/ahm/tasks.go`
- `internal/ahm/indexes.go`
- `docs/cli.md`
- `.agents/TASKS.md`
- Task command tests under `internal/ahm/`

## Fix Direction

Design and implement a small label API for task commands. Candidate behavior:

- `ahm task list --label <label>` with repeatable or comma-separated labels.
- A ready-work filter that combines queue readiness with one or more labels.
- `ahm task labels` or equivalent to list labels used by active tasks, with counts.

Keep the command shape boring and scriptable. Avoid inventing a large query language unless a concrete use case forces it.

## Acceptance Notes

- [x] `ahm task list --label <label>` supports repeated and comma-separated label filters with AND semantics.
- [x] `ahm task ready --label <label>` lists ready work filtered by one or more labels.
- [x] `ahm task labels` lists repository labels with deterministic counts for total tasks, active-bucket tasks, Open tasks, and ready tasks.
- [x] Added focused task command tests for label filtering, ready label filtering, label summary counts, help output, and malformed-task resilience.
- [x] Updated `docs/cli.md`, `.agents/TASKS.md`, and `internal/templates/workflow/TASKS.md` with the label workflow.
- [x] Verification: `go test ./internal/ahm -run 'TestTask(ListFiltersLabels|ReadyFiltersLabels|LabelsListsCounts|ListFiltersStatus|CommandsResilientToMalformedTasks|NestedHelp)'`; `go test ./internal/templates ./internal/ahm`; `just ci`.

