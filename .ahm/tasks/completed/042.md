---
id: 042
title: Normalize exec plan field in one layer only
status: Completed
priority: P3
effort: XS
labels: type:refactor, area:tasks
exec_plan: -
depends_on: -
updated: 2026-06-06T21:22:06-04:00
---
# Normalize exec plan field in one layer only

## Summary

Review issue 42. exec_plan is normalized with defaultDash in both parseTask and renderTask.

## Decision

Approach: **Normalize in parse (read path) only; remove `defaultDash` from render.**

This keeps `exec_plan` consistent with every other `defaultDash` field (Status,
Priority, Effort, Labels) — all are normalized in `parseTaskFromData` and trusted
by `renderTask` without re-normalization.

Change needed: remove the `defaultDash()` wrapper around `task.ExecPlan` on the
`renderTask` line that writes `exec_plan`. All other render lines for defaultDash
fields already trust the struct value.

## Acceptance Notes

- [x] `defaultDash` removed from the `exec_plan` write in `renderTask`.
- [x] Parse still normalizes `exec_plan` via `defaultDash` in `parseTaskFromData`.
- [x] Rendered output is identical for all valid task files (no behavior change).
- [x] Tests confirm that a task with empty `exec_plan` front matter renders as `-`.
- [x] Tests confirm that a task with `-` explicitly in `exec_plan` front matter round-trips to `-`.
- [x] `go test ./internal/ahm` passes.
- [x] `just ci` is green before handoff.

