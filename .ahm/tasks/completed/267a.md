---
id: 267a
title: Derive project identity and resolve the home store root
status: Completed
priority: P1
effort: M
labels: type:feature, area:workflow
depends_on: -
created: 2026-09-21T11:29:22-04:00
updated: 2026-09-21T12:41:24-04:00
parent: 267
---
# Derive project identity and resolve the home store root

## Summary

Task records need a project identity before they can live outside the project.
Implement the derivation from ADR 023 and make it observable.

Add `internal/ahm/identity.go`:

    func canonicalRemoteKey(rawRemote string) (key string, ok bool)
    func canonicalPathKey(projectRoot string) (key string, err error)
    func projectKeyFor(projectRoot string) (key string, kind string, rawRemote string, err error)
    func storeDirName(key string) string

`canonicalRemoteKey` returns the lowercased `host/owner/repo` form: scheme,
userinfo, default port, trailing `.git`, and trailing slash removed, scp-like
syntax accepted, non-default ports kept, and credentials never hashed or
persisted. It reports `ok == false` for `file://` URLs and local-path remotes
so the caller uses the path rule. `canonicalPathKey` hashes the
symlink-resolved absolute project root with SHA-256. `storeDirName` returns
`<slug>-<hash8>`.

The remote is read with `git remote get-url origin` through
`cleanGitEnvironment` in `internal/ahm/git.go`, cached once per command. Use
`origin`; otherwise a sole remote; several remotes without `origin` fall back
to the path rule rather than guessing.

Add `internal/ahm/store.go` with `storeRoot`, `resolveStore`, the registry
load and write helpers, and the store's `project.json` state file, per the
Interfaces section of the ExecPlan. `AHM_HOME` must be absolute; reject a
relative value.

Add the `store` command group in `internal/ahm/cli.go` with one subcommand,
`path`, printing the root, key, and records directory, and the same fields
under `--json`. Register it in the CLI reference docs that this milestone
touches.

## Acceptance

- `internal/ahm/identity_test.go` covers a golden table: scp-like and URL
  remotes for one repository produce one key; uppercase is lowercased;
  credentials are stripped; a default port is dropped and a non-default port
  is kept; `file://` and local-path remotes take the path rule; two clones and
  a symlinked path resolve to one key.
- `ahm store path` prints the same key and directory from two clones of one
  repository, and a `path`-kind key in a repository with no remote.
- `AHM_HOME` redirects the store root; a relative value exits 2.
- `just ci` passes.

## Comments

**2026-09-21T12:41:24-04:00** — 267a landed. New internal/ahm/identity.go (canonicalRemoteKey, canonicalPathKey, projectKeyFor, storeDirName, redactRemote) and internal/ahm/store.go (storeRoot, resolveStore, registry and project.json state, storePathReport, the store group), plus readGitRemote/runGit in git.go and the ahm store path command. Identity uses the canonical origin URL (host/owner/repo, lowercased, no scheme/credentials/default port/.git), the sole non-origin remote, else the SHA-256 of the symlink-resolved project root. Two clones and a linked path resolve to one key; a path key uses a project- slug.

Judgment call to confirm: ahm store path records the observation — a registry entry and project.json — unless --dry-run, so identity data accumulates before a project migrates (the plan asked for the registry write helpers and the state file, and this is the only store command before 267c). Both files are written only when their bytes change; a repeated run writes nothing.

Three subagent review rounds; all findings fixed. The notable one: an unreadable or broken Git repository now fails instead of silently resolving a different path key, and identity uses the project root's own .git, so a nested managed root does not inherit an outer remote. Deferred and recorded in the ExecPlan: the registry has no lock yet (Surprises), ADR 023's casing claim and its absolute-path display rule need wording in 267h, and 267c must keep store resolution lazy in project mode so a Git read failure cannot break repositories that never read the store.

Verified: just ci green (gofmt, tidy, vet, race, golangci-lint, govulncheck, markdownlint, GoReleaser snapshot); ahm doctor reports no findings; two clones of one repository print the same key and directory; a repository with no remote prints a path key; AHM_HOME redirects the root and a relative value exits 2. Nothing is committed: the change is 10 modified and 4 new files in the worktree.

